global:
  scrape_interval: 10s # Slightly longer for Prod to save resources

scrape_configs:
  - job_name: 'mern-stack-prod'
    static_configs:
      - targets: ['server:4000'] 
    # DNS service discovery allows Prometheus to see all 3 server replicas
